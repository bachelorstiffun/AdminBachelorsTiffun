<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.touchizen.adminbachelorstiffun">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Admin Bachelors Tiffun"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".AllMenuActivity.ShowAllMenuActivity"
            android:exported="false" />
        <activity
            android:name=".AllMenuActivity.AddMenuActivity"
            android:exported="false" />
        <activity
            android:name=".AllMenuActivity.SetTodaysMenuActivity"
            android:exported="false" />
        <activity
            android:name="com.touchizen.adminbachelorstiffun.Login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.touchizen.adminbachelorstiffun.MainActivity"
            android:label="Admin Bachelors Tiffun"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            </intent-filter>
        </activity>
    </application>

</manifest>